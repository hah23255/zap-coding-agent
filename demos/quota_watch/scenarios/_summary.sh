#!/usr/bin/env bash
sleep 1

Y='\033[38;5;220m'
G='\033[38;5;114m'
C='\033[38;5;81m'
D='\033[38;5;240m'
B='\033[1m'
N='\033[0m'
S='\033[38;5;246m'

printf '\n'
printf "  ${Y}━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━${N}\n"
printf '\n'
printf "  ${B}${Y}⚡ zap  ·  Usage-window watcher${N}\n"
printf '\n'
printf "  ${C}Codex${N}  ${S}→ real ${N}${B}x-codex-primary/secondary-used-percent${N}${S} response headers${N}\n"
printf "  ${S}         checked on every reply — always accurate, always live.${N}\n"
printf '\n'
printf "  ${C}Claude${N} ${S}→ no official CLI flag exists yet — so zap reads the same${N}\n"
printf "  ${S}         ${N}${B}rate_limits${N}${S} data Claude Code's own statusLine feature uses,${N}\n"
printf "  ${S}         rechecked every 5 minutes.${N}\n"
printf '\n'
printf "  ${G}✓${N}  ${S}Live 5h / 7d %% in the sidebar, color-coded${N}\n"
printf "  ${G}✓${N}  ${S}One-time warning banner at 80%% usage — switch providers before you hit the wall${N}\n"
printf "  ${G}✓${N}  ${S}Best-effort by design: never blocks or breaks a real turn${N}\n"
printf '\n'
printf "  ${D}────────────────────────────────────────────────────────────────────${N}\n"
printf '\n'
printf "  ${B}One window. Two subscriptions. Zero surprises.${N}\n"
printf '\n'
printf "  ${D}github.com/zap-coding-agent/zap-coding-agent${N}\n"
printf '\n'
printf "  ${Y}━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━${N}\n"
printf '\n'
